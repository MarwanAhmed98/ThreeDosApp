import { SearchcouncilsPipe } from './searchcouncils.pipe';

describe('SearchcouncilsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcouncilsPipe();
    expect(pipe).toBeTruthy();
  });
});
