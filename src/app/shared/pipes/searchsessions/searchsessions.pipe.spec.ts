import { SearchsessionsPipe } from './searchsessions.pipe';

describe('SearchsessionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsessionsPipe();
    expect(pipe).toBeTruthy();
  });
});
