import { SearchtasksPipe } from './searchtasks.pipe';

describe('SearchtasksPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtasksPipe();
    expect(pipe).toBeTruthy();
  });
});
