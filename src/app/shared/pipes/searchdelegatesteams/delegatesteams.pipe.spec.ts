import { DelegatesteamsPipe } from './delegatesteams.pipe';

describe('DelegatesteamsPipe', () => {
  it('create an instance', () => {
    const pipe = new DelegatesteamsPipe();
    expect(pipe).toBeTruthy();
  });
});
